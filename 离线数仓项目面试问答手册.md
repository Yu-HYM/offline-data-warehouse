# 离线数仓项目面试问答手册

> **项目名称**：电商离线数据仓库  
> **技术栈**：Hadoop 3.3.6 + Hive 3.1.3 + MySQL 8.0  
> **项目定位**：从 MySQL 业务库抽取订单/用户/商品数据，经 ETL 清洗后写入 Hive 数据仓库，支撑离线报表分析

---

## 一、项目整体介绍

### Q1: 请简单介绍一下你的离线数仓项目？

> **参考回答**：
> 这是一个**电商离线数据仓库**项目。核心流程是从 MySQL 业务库中抽取订单、用户、商品数据，经过 ETL 清洗后写入 Hive 数据仓库的 ODS 贴源层，再通过 Hive SQL 进行分层加工，最终支撑离线报表和指标分析。
>
> **技术架构**：
> ```
> MySQL业务库 → Python/脚本ETL → HDFS(ODS贴源层) → Hive SQL加工 → ADS应用层
> ```
>
> **核心组件**：
> - Hadoop（HDFS + YARN）：分布式存储与资源调度
> - Hive 3.1.3：数据仓库引擎，支持 SQL 化操作 HDFS 数据
> - MySQL 8.0：业务库 + Hive Metastore 元数据库
> - Python (pymysql)：实现 ETL 数据同步

---

## 二、Hadoop 核心知识

### Q2: Hadoop 的核心组件有哪些？分别有什么作用？

> **参考回答**：
> | 组件 | 作用 | 本项目配置 |
> |---|---|---|
> | **HDFS** | 分布式存储系统，由 NameNode（管理文件元信息）和 DataNode（存储实际数据）组成 | 单节点伪分布式，默认端口 9000 |
> | **YARN** | 资源调度框架，ResourceManager 负责全局调度，NodeManager 负责节点管理 | 管理 Hive 任务的计算资源 |
> | **MapReduce** | 离线批处理计算引擎，分 Map（读取+处理）和 Reduce（聚合）两个阶段 | 被 Hive 翻译执行 |

### Q3: Hadoop 伪分布式和全分布式有什么区别？

> **参考回答**：
> | 对比项 | 伪分布式 | 全分布式 |
> |---|---|---|
> | 节点数 | 1 台 | 多台 |
> | 进程分布 | NameNode/DataNode/ResourceManager/NodeManager 全在一台 | 分散在不同机器 |
> | 适用场景 | 开发测试 | 生产环境 |
> | 额外配置 | 无 | SSH 免密、时间同步、core-site 配置 |
>
> 本项目采用伪分布式，所有进程通过 `jps` 命令可查看到：NameNode、DataNode、SecondaryNameNode、ResourceManager、NodeManager。

### Q4: HDFS 的读写流程是怎样的？

> **参考回答**：
> - **读流程**：Client → NameNode（获取 Block 位置）→ DataNode（按顺序读取 Block）
> - **写流程**：Client → NameNode（分配 Block）→ DataNode Pipeline（依次写入，默认 3 副本）

---

## 三、Hive 核心知识

### Q5: Hive 和 MySQL 有什么核心区别？

> **参考回答**：
> | 维度 | Hive | MySQL |
> |---|---|---|
> | 设计类型 | OLAP（分析型） | OLTP（事务型） |
> | 数据存储 | HDFS 列存储 | 本地磁盘行存储 |
> | 数据更新 | 不支持行级更新 | 支持 |
> | 索引支持 | 无 | B+ Tree 索引 |
> | 适用场景 | 海量数据离线批处理 | 高并发在线事务 |
> | 查询延迟 | 分钟/小时级 | 毫秒级 |
> | 底层执行 | MapReduce/Tez/Spark | 存储引擎直接执行 |

### Q6: Hive 的 Metastore 为什么用 MySQL 而不用默认的 Derby？

> **参考回答**：
> | 对比项 | Derby（默认） | MySQL |
> |---|---|---|
> | 数据库类型 | 内嵌式，无需独立部署 | 独立数据库服务 |
> | 并发支持 | 不支持多会话并发 | 支持多客户端并发访问 |
> | 可靠性 | 数据容易丢失 | 稳定可靠 |
> | 适用场景 | 单机测试 | 生产环境 |
>
> 配置文件：`hive-site.xml` 中 `javax.jdo.option.ConnectionURL` 指向 MySQL 的 `metastore` 库。

### Q7: Hive 内部表和外部表有什么区别？

> **参考回答**：
> | 对比项 | 内部表 | 外部表 |
> |---|---|---|
> | 删除行为 | 删表时**数据和表一起删除** | 只删除表定义，**HDFS 数据保留** |
> | 数据管理 | Hive 完全管理生命周期 | 数据由外部管理 |
> | 适用场景 | 临时数据、中间结果 | 共享数据、多表共用数据源 |
> | 本项目使用 | - | 使用外部表，数据直接关联 HDFS 目录 |

### Q8: Hive 分区是什么？有什么好处？

> **参考回答**：
> **分区**是在 HDFS 上按目录组织数据的方式。例如按日期分区：
> ```
> /warehouse/mall/order_info/dt=2026-08-20/
> /warehouse/mall/order_info/dt=2026-08-21/
> ```
>
> **好处**：
> 1. **减少全表扫描**：查询指定分区时只扫描相关目录
> 2. **提升查询性能**：避免扫描无关数据
> 3. **便于数据管理**：按分区进行数据生命周期管理（删除过期分区等）

### Q9: Hive 的存储格式有哪些？该怎么选？

> **参考回答**：
> | 格式 | 特点 | 适用场景 |
> |---|---|---|
> | **TEXTFILE** | 纯文本，可读性好，压缩比低 | 入门/测试场景 |
> | **ORC** | 列式存储，高压缩比，带索引 | 生产环境推荐 |
> | **Parquet** | 列式存储，跨语言兼容 | 多引擎（Spark/Impala）共用 |
>
> 本项目使用 TEXTFILE 方便演示，生产建议 ORC 格式。

---

## 四、数据同步（ETL）相关

### Q10: 你们是怎么做 MySQL 到 HDFS 的数据同步的？

> **参考回答**：
> 生产环境使用 **DataX**（阿里云开源的离线数据同步工具），支持 30+ 数据源的互相同步。本项目用 Python (pymysql) 实现了等效的 ETL 逻辑：
>
> **三步流程**：
> 1. **抽取（Extract）**：`pymysql.connect()` 连接 MySQL，`SELECT * FROM table` 读取数据
> 2. **转换（Transform）**：空值处理（None→空串）、类型转换、字段映射
> 3. **加载（Load）**：写入 CSV 文件，`hdfs dfs -put` 上传到 HDFS，Hive 建外部表关联

### Q11: 全量同步和增量同步有什么区别？怎么选？

> **参考回答**：
> | 同步方式 | 实现逻辑 | 适用场景 |
> |---|---|---|
> | **全量同步** | `SELECT * FROM table`，每次同步所有数据 | 数据量小、变化频率低的表 |
> | **增量同步** | `WHERE update_time > '上次同步时间'` | 数据量大、变化频繁的表 |
> | **基于 binlog** | 解析 MySQL 二进制日志获取变更 | 近实时同步需求 |
>
> 实际项目中通常 **全量 + 增量结合**：小表全量，大表增量。

### Q12: DataX 有哪些核心插件？

> **参考回答**：
> DataX 的插件体系分为 Reader（读）和 Writer（写）：
> - **mysqlreader / mysqlwriter**：MySQL 数据源
> - **hdfsreader / hdfswriter**：HDFS 数据源
> - **hivreader / hivewriter**：Hive 数据源
> - **txtfilereader / txtfilewriter**：文本文件
> - **streamwriter**：输出到控制台（调试用）

---

## 五、数据仓库分层

### Q13: 数据仓库应该分哪几层？

> **参考回答**：
> ```
> ┌─────────────────────────────────────┐
> │  ADS 应用数据层                      │  ← mall_ads 数据库
> │  （面向业务报表、看板、API）           │
> ├─────────────────────────────────────┤
> │  DWS 汇总数据层                      │
> │  （主题宽表、按天/月聚合）             │
> ├─────────────────────────────────────┤
> │  DWD 明细数据层                      │
> │  （清洗后的标准化明细记录）             │
> ├─────────────────────────────────────┤
> │  ODS 贴源数据层                      │  ← mall 数据库
> │  （原始数据 1:1 保留，不做修改）        │
> └─────────────────────────────────────┘
> ```

### Q14: 为什么要做数据仓库分层？

> **参考回答**：
> 1. **解耦**：源系统变更只改 ODS 层，不影响上层逻辑
> 2. **复用**：DWD 明细层可被多个 ADS 指标复用，避免重复计算
> 3. **可追溯**：数据异常时可从 ADS → ODS 逐层回溯定位问题
> 4. **效率**：汇总层（DWS）预计算，查询时直接读取结果，提升性能
> 5. **标准化**：统一字段命名、数据类型、编码规范

---

## 六、项目实战细节

### Q15: 项目的完整数据流向是怎样的？

> **参考回答**：
> ```
> ① MySQL 业务库
>    mall.user_info (8条)
>    mall.product_info (8条)
>    mall.order_info (10条)
>         │
>         ▼  ② Python ETL 抽取
>    datax_sync.py (pymysql)
>    输出 CSV 文件到 /home/hadoop/mall_ods/
>         │
>         ▼  ③ 上传到 HDFS
>    hdfs dfs -put *.csv /warehouse/mall/{表名}/
>         │
>         ▼  ④ Hive 建外部表
>    CREATE EXTERNAL TABLE mall.user_info ...
>    LOCATION 'hdfs://localhost:9000/warehouse/mall/user_info'
>         │
>         ▼  ⑤ Hive SQL 查询
>    SELECT * FROM mall.user_info WHERE city='北京'
>    → 引擎翻译为 MapReduce → HDFS 读取 → 返回结果
> ```

### Q16: 项目中遇到了哪些技术难点？怎么解决的？

> **参考回答**：
> | 问题 | 原因 | 解决方案 |
> |---|---|---|
> | Hive 启动报 `JAVA_HOME not set` | WSL 非交互式 shell 不加载 `.bashrc` | 在执行命令前直接 `export JAVA_HOME=...` |
> | Hive 建表报 `MetaException: warehouse 创建失败` | `warehouse.dir` 路径缺少 `hdfs://` 前缀 | 改为完整 URI：`hdfs://localhost:9000/warehouse` |
> | Hive 报 SLF4J 多绑定警告 | Hadoop 和 Hive 带了重复的 guava/slf4j 包 | 删除 Hive lib 中旧版本 jar |
> | Hive 连接 MySQL 报 `ClassNotFoundException` | 缺少 MySQL JDBC 驱动 | 下载 `mysql-connector-j-8.0.33.jar` 放到 Hive lib 目录 |
> | PowerShell 执行 WSL 命令引号嵌套错误 | Windows/Linux 环境差异 | 改用 `.sql`/`.py` 文件方式执行，避免复杂引号转义 |

### Q17: Hive 的 MapReduce 任务为什么显示 `Running in-process`？

> **参考回答**：
> 当数据量很小（本项目只有几十行数据），Hive 会使用 **Local Mode**（本地模式），在 JVM 内直接执行，不启动 YARN 任务。这是 Hive 的自动优化策略，避免小任务的启动开销。
>
> 可以通过参数控制：
> ```sql
> SET hive.exec.mode.local.auto=true;   -- 自动启用本地模式
> SET hive.exec.mode.local.auto.inputbytes.max=134217728;  -- 最大 128MB
> ```

---

## 七、数据生成（造数）策略

### Q18: 项目中为什么需要自己造数？

> **参考回答**：
> 真实项目中数据由业务系统产生，但做 Demo/面试项目时需要自己模拟。造数要注意：
> 1. **数据量级合理**：用户 1000+、商品 200、订单 6000+，符合电商业务体量
> 2. **覆盖各种场景**：订单状态覆盖待支付/已支付/已发货/已完成/已取消
> 3. **刻意注入脏数据**：约 5% 比例，测试清洗逻辑
> 4. **包含变更场景**：驱动 SCD2 拉链表测试

### Q19: 脏数据是怎么注入的？为什么要这么做？

> **参考回答**：
> **注入策略**（约 5% 比例）：
> | 脏数据类型 | 比例 | 处理方式 |
> |---|---|---|
> | 空值（None） | 2% | DWD 层默认值填充 |
> | 负值/非法值 | 1.5% | 数值置 0，字符串用"异常"标记 |
> | 非法时间 | 1.5% | 过滤或使用创建时间替代 |
>
> **目的**：模拟真实业务中的数据质量问题，验证 DWD 层清洗逻辑的完备性。面试时可以说："我们提前在源数据中注入了约 5% 的脏数据，来测试 DWD 层的清洗规则是否完善。"

### Q20: SCD2 拉链表是怎么造出来的？

> **参考回答**：
> **造数逻辑**：在每日增量造数时，随机选择 5-10 个老用户，修改其手机号（触发变更）。
>
> **拉链处理**（DWD 层实现）：
> ```sql
> -- 1. 关闭旧链
> UPDATE dim_user_full
> SET end_date = '变更日期-1', is_current = 0
> WHERE user_id = ? AND is_current = 1
>
> -- 2. 开新链
> INSERT INTO dim_user_full (user_id, phone, start_date, end_date, is_current)
> VALUES (?, '新手机号', '变更日期', '9999-12-31', 1)
> ```
>
> 面试时可以说："我们用 Python 脚本模拟用户变更，在 DWD 层用 SQL 实现 SCD2 拉链逻辑，保留每个版本的生效时间区间。"

### Q21: 手机号脱敏是怎么做的？

> **参考回答**：
> ```python
> def mask_phone(phone):
>     if phone is None or len(phone) != 11:
>         return '00000000000'  # 默认值
>     return phone[:3] + '****' + phone[7:]  # 中间4位用*替代
> ```
> **脱敏结果**：`13812345678` → `138****5678`
>
> 面试时可以说："手机号属于敏感数据，在 DWD 层统一脱敏处理，既保留了数据格式，又保护了用户隐私。"

---

## 八、ODS 贴源层同步

### Q22: 全量同步和增量同步是怎么选择的？

> **参考回答**：
> | 表类型 | 同步方式 | 原因 |
> |---|---|---|
> | **user_info** | 首日全量 + 每日增量 | 用户量小，变更频率低 |
> | **sku_info** | 全量覆盖 | 商品表小（200条），全量比增量更简单 |
> | **order_info** | 按 create_time 增量 | 订单量大（6000+），按天切分高效 |
> | **order_detail** | 按 create_time 增量 | 明细跟随订单 |
> | **payment_info** | 按 create_time 增量 | 支付跟随订单 |
>
> **选择原则**：①小表（<1万行）全量，大表增量 ②维度表全量覆盖，事实表按时间增量 ③每天调度，按 dt 分区

### Q23: 分区键为什么选 create_time 而不是 update_time？

> **参考回答**：
> | 对比项 | create_time | update_time |
> |---|---|---|
> | 稳定性 | 订单创建后不变 | 状态变更会更新 |
> | 完整性 | 所有订单都有 | 部分订单可能未更新 |
> | 性能 | 按创建时间切分，查询当天订单只需扫描一个分区 | 可能扫描多个分区 |
> | 幂等性 | 同一天订单固定，可重复覆盖 | 同一天数据可能变化 |
>
> **结论**：事实表（订单/支付）用 create_time，维度表（用户）用 operate_time 捕获变更。

### Q24: ODS 层为什么用外部表而不是内部表？

> **参考回答**：
> | 对比项 | 外部表 | 内部表 |
> |---|---|---|
> | 数据位置 | HDFS 指定目录 | Hive 仓库目录 |
> | 删除行为 | DROP TABLE 只删元数据，HDFS 数据保留 | DROP TABLE 同时删除数据 |
> | 数据管理 | ETL 工具管理（DataX/Python） | Hive 管理 |
> | 适用场景 | ODS 贴源层 | DWD/DWS/ADS 层 |
>
> **原因**：ODS 层数据由 ETL 工具写入，Hive 只负责查询。如果误删 Hive 表，HDFS 数据不受影响，可以重新建表恢复。

### Q25: ODS 层数据格式为什么选 TextFile？

> **参考回答**：
> | 格式 | 特点 | 适用场景 |
> |---|---|---|
> | **TextFile** | 纯文本，可读性好 | ODS 贴源层 |
> | **ORC** | 列式存储，高压缩比 | DWD/DWS/ADS 层 |
> | **Parquet** | 跨语言兼容 | 多引擎共用 |
>
> **选择 TextFile 的原因**：
> 1. ODS 层数据量小（几 MB），压缩比不重要
> 2. 可读性优先，便于排错和数据校验
> 3. 后续 DWD 层会转换为 ORC 格式

---

## 九、DWD 明细层加工

### Q26: DWD 层的清洗规则有哪些？

> **参考回答**：
> | 清洗规则 | 处理方式 | 示例 |
> |---|---|---|
> | 空值处理 | 默认值填充 | None → '00000000000'（手机号）/ 0（金额） |
> | 负值过滤 | WHERE 条件过滤 | `total_amount > 0` 过滤 113 条负金额订单 |
> | 非法时间 | 用创建时间替代 | 1970-01-01 → create_time |
> | 性别标准化 | 字符串→数字 | '男'→1, '女'→2, 其他→0 |
> | 金额精度 | 保留 2 位小数 | round(amount, 2) |

### Q27: 什么是维度退化？为什么要做？

> **参考回答**：
> **定义**：将维度表的描述性属性直接冗余到事实表中。
>
> **示例**：
> ```sql
> -- 原始方式（需要 JOIN）
> SELECT o.*, u.nick_name, s.sku_name 
> FROM order_info o 
> JOIN user_info u ON o.user_id = u.id
> JOIN sku_info s ON o.sku_id = s.id
>
> -- 维度退化后（直接查询）
> SELECT * FROM dwd_fact_order_detail
> -- 已包含 user_nick_name, sku_name, category1_name 等
> ```
>
> **好处**：
> 1. **减少 JOIN**：下游查询不需要再关联维度表
> 2. **提升性能**：宽表扫描比多表 JOIN 更快
> 3. **历史快照**：保留下单时的商品名称，即使后续改价也不影响历史数据

### Q28: SCD2 拉链表的核心逻辑是什么？

> **参考回答**：
> **两个核心字段**：
> - `start_date`：版本生效日期
> - `end_date`：版本失效日期（9999-12-31 表示当前版本）
>
> **处理逻辑**：
> | 场景 | 处理方式 |
> |---|---|
> | 新用户首次出现 | INSERT，start_date=创建日期，end_date=9999-12-31，is_current=1 |
> | 老用户信息变更 | ①关闭旧链：UPDATE end_date=变更日期-1，is_current=0 |
> | | ②开新链：INSERT 新记录，start_date=变更日期，end_date=9999-12-31，is_current=1 |
>
> **查询当前版本**：`WHERE is_current = 1`
> **查询历史版本**：`WHERE start_date <= '查询日期' AND end_date >= '查询日期'`

### Q29: 订单明细表为什么要关联这么多表？

> **参考回答**：
> 订单明细事实表是**数仓的核心事实表**，承载了大部分业务分析需求：
>
> | 关联表 | 关联目的 | 分析场景 |
> |---|---|---|
> | user_info | 获取用户属性 | 不同等级用户的消费差异 |
> | sku_info | 获取商品属性 | 不同分类的销售情况 |
> | payment_info | 获取支付状态 | 支付转化率分析 |
>
> **在 DWD 层一次关联完成**，下游 ADS 层直接查询宽表，避免重复 JOIN。

### Q30: 项目中的 DWD 层数据量是多少？

> **参考回答**：
> | 表 | 2026-08-19（全量） | 2026-08-20（增量） |
> |---|---|---|
> | dim_user_full | 1120 行 | 17 行 |
> | dim_sku_full | 200 行 | 200 行 |
> | fact_order_detail | 388 行 | 328 行 |
> | fact_payment_detail | 108 行 | 96 行 |
>
> **面试技巧**：说具体数据量，显得真实可信。可以补充："首日全量初始化，之后每天增量处理，整体数据量不大但覆盖了完整的业务场景。"

---

## 十、DWS 汇总层与 ADS 应用层

### Q31: DWS 层的职责是什么？和 DWD 有什么区别？

> **参考回答**：
> | 对比项 | DWD 明细层 | DWS 汇总层 |
> |---|---|---|
> | 粒度 | 一行一笔订单明细 | 一行一个用户/商品/天 |
> | 作用 | 清洗 + 维度退化 | 轻度汇总，形成主题宽表 |
> | 典型表 | fact_order_detail | dws_user_day_agg |
> | 关联 | 多表 JOIN 形成宽表 | 按主题聚合，不再关联 |
>
> **DWS 核心职责**：
> 1. **按天聚合**：将明细数据按用户/商品/分类聚合
> 2. **形成主题**：用户主题、商品主题、订单主题
> 3. **轻度汇总**：不需要复杂计算，只是 GROUP BY 聚合

### Q32: ADS 层的职责是什么？

> **参考回答**：
> ADS（Application Data Service）层是面向应用的最终结果表，直接供 BI 报表、数据看板、产品决策使用。
>
> **特点**：
> 1. **高度汇总**：每日一行或每月一行
> 2. **业务导向**：指标命名贴近业务（GMV、ARPU、支付率）
> 3. **开箱即用**：不需要再 JOIN，可以直接被 BI 工具查询
>
> **本项目 ADS 表**：
> | 表名 | 用途 | 使用者 |
> |---|---|---|
> | ads_gmv_day | GMV 日报 | 运营/财务 |
> | ads_user_dashboard | 用户看板 | 产品/运营 |
> | ads_sku_sales_top | 商品销售榜 | 运营/采购 |
> | ads_user_rfm | 用户分层 | 营销/CRM |

### Q33: 什么是 RFM 模型？本项目是怎么实现的？

> **参考回答**：
> **RFM 模型**是用户价值分析的经典模型：
> - **R (Recency)**：最近一次消费时间，越近分越高
> - **F (Frequency)**：消费频次，越多分越高
> - **M (Monetary)**：消费金额，越多分越高
>
> **本项目实现**：
> 1. 取近 30 天订单数据
> 2. 计算每个用户的 R、F、M 三个指标
> 3. 按规则打分 1-5 分
> 4. 根据三个分数组合分层：
>    - 高 R 高 F：重要价值客户
>    - 高 R 低 F：重要发展客户
>    - 低 R 高 F：重要保持客户
>    - 低 R 低 F：重要挽留客户
>
> **面试话术**："我们用 RFM 模型做用户分层，基于近 30 天数据给每个用户打 R/F/M 三个分，再按分数组合分成 6 类客户群，给运营提供精准营销依据。"

### Q34: GMV 是什么？怎么计算的？

> **参考回答**：
> **GMV（Gross Merchandise Volume）**：商品交易总额，衡量电商平台规模的核心指标。
>
> **计算公式**：`GMV = 所有订单金额总和（包括未支付）`
>
> **相关指标**：
> | 指标 | 公式 | 说明 |
> |---|---|---|
> | GMV | SUM(order_amount) | 总交易额（含未支付） |
> | 实际支付金额 | SUM(paid_amount) | 真实到账金额 |
> | 客单价 | GMV / 订单数 | 平均每单金额 |
> | 支付率 | 已支付订单数 / 总订单数 | 支付转化能力 |
>
> **本项目示例**：2026-08-19 GMV ¥227,429，支付率 66%

### Q35: 数据仓库分层的完整链路是什么？

> **参考回答**：
> ```
> 业务库 (MySQL)
>     ↓ ETL/DataX
> ODS 贴源层 (HDFS)  ← 原样同步，不做加工
>     ↓ DWD 清洗
> DWD 明细层 (HDFS)  ← 清洗 + 维度退化 + SCD2
>     ↓ DWS 聚合
> DWS 汇总层 (HDFS)  ← 按主题轻度汇总
>     ↓ ADS 加工
> ADS 应用层 (HDFS)  ← 面向业务的最终结果
>     ↓
> BI 看板 / 数据产品
> ```
>
> **面试要点**：强调"单向流动、逐层加工"，每层职责清晰，上层依赖下层。

### Q36: 本项目各层数据量是多少？

> **参考回答**：
> | 层级 | 表数 | 数据量 | 说明 |
> |---|---|---|---|
> | ODS | 5 张 | 1120 用户 + 200 商品 + 6500 订单 | 贴源层，按天分区 |
> | DWD | 4 张 | 1120 用户 + 200 商品 + 716 订单明细/天 | 清洗+维度退化 |
> | DWS | 4 张 | 140 用户汇总 + 160 商品汇总/天 | 按主题聚合 |
> | ADS | 4 张 | 1 行/天（GMV）+ 900+ 用户 RFM | 面向业务 |
>
> **面试技巧**：说具体数据量 + 对比各层变化，体现对数据流转的理解。

### Q37: DWS 层为什么用 ORC 内部表而不是外部表？

> **参考回答**：
> | 对比项 | 外部表 (TextFile) | 内部表 (ORC) |
> |---|---|---|
> | 存储格式 | 文本格式，可读性好 | 列式存储，压缩比高 |
> | 查询性能 | 慢，需扫描整行 | 快，只读取需要的列 |
> | 压缩 | 无压缩或低压缩 | Snappy 压缩，节省 50%+ 空间 |
> | 生命周期 | 适合临时/中间数据 | 适合长期存储的结果数据 |
> | 场景 | ODS 贴源层、临时过渡 | DWS/ADS 结果层 |
>
> **本项目实践**：
> - ODS 层用 TextFile 外部表：数据来自 MySQL，需要可读、易验证
> - DWS 层用 ORC 内部表：聚合后数据稳定，查询频繁，需要高性能
>
> **面试话术**："ODS 层用外部表方便贴源数据验证，DWS 层转 ORC 内部表提升查询性能和压缩率，是数仓分层的最佳实践。"

### Q38: Hive 内部表和外部表的加载方式有什么不同？

> **参考回答**：
> | 方式 | 适用场景 | 示例 |
> |---|---|---|
> | LOAD DATA | 快速加载小文件，不触发 MR | 从本地/小文件直接导入 |
> | INSERT OVERWRITE | 从其他表转换格式，触发 MR | 从外部表转 ORC 内部表 |
> | CREATE TABLE AS | 建表+查询一步完成 | CTAS 快速建表 |
>
> **本项目实践**：
> ```sql
> -- 步骤1: Python 聚合数据到 HDFS（TextFile 格式）
> -- 步骤2: 创建临时外部表指向 HDFS 路径
> CREATE EXTERNAL TABLE tmp_user_dws_text(...) 
> STORED AS TEXTFILE LOCATION '/path/to/data';
> 
> -- 步骤3: INSERT OVERWRITE 转换为 ORC 内部表
> INSERT OVERWRITE TABLE dws_trade_user_order_1d PARTITION (dt='...')
> SELECT * FROM tmp_user_dws_text;
> 
> -- 步骤4: 清理临时表
> DROP TABLE tmp_user_dws_text;
> ```

### Q39: 什么是 Hive 本地模式？怎么优化小数据量查询？

> **参考回答**：
> **Hive 本地模式**：对于小数据量查询，Hive 可以不启动 MapReduce 任务，直接在本地 JVM 中执行，速度更快。
>
> **启用本地模式**：
> ```sql
> SET hive.exec.mode.local.auto = true;              -- 自动启用
> SET hive.exec.mode.local.auto.inputbytes.max = 128000000;  -- 128MB 以下启用
> SET hive.exec.mode.local.auto.input.files.max = 10;         -- 10 个文件以下启用
> ```
>
> **适用场景**：
> - 小表加载（< 128MB）
> - 临时数据转换
> - 测试/调试 SQL
>
> **面试话术**："对于小数据量查询，我们开启 Hive 本地模式，避免启动 MR 任务的开销，查询速度可以提升 10 倍以上。"

### Q40: DWS 聚合宽表的维度退化是怎么实现的？

> **参考回答**：
> **维度退化**：将维度表（如用户表、商品表）的属性冗余到事实表中，避免下游查询时频繁 JOIN。
>
> **本项目实现**：
> ```sql
> -- 不做维度退化（下游需要 JOIN）
> SELECT o.*, u.nick_name, s.sku_name
> FROM dwd_trade_order_info_inc o
> LEFT JOIN dim_user_full u ON o.user_id = u.id
> LEFT JOIN dim_sku_full s ON o.sku_id = s.id;
> 
> -- 维度退化后（DWS 宽表已冗余维度属性）
> SELECT * FROM dws_trade_user_order_1d WHERE dt='2026-08-20';
> ```
>
> **退化的维度属性**：
> | 原维度表 | 退化到 DWS 的属性 |
> |---|---|
> | user_info | nick_name, user_level, gender |
> | sku_info + category | sku_name, category1/2/3_name |
>
> **好处**：减少下游 JOIN 操作，提升查询性能，降低使用门槛。

---

## 十、ADS 指标层（阶段七）

### Q41: ADS 指标层有哪些表？分别计算什么指标？

> **参考回答**：
> ADS（Application Data Service）是数仓最终结果层，直接对接 BI 报表/仪表盘。本项目建了 3 张 ORC 内部表（SNAPPY 压缩，按 dt 分区）：
>
> | 表名 | 指标 | 加工方式 | 数据量 |
> |---|---|---|---|
> | ads_trade_stats | GMV、订单数、支付用户数、客单价 | Hive 本地模式聚合 DWS | 2 天 |
> | ads_repurchase_rate | 当日下单用户中累计下单≥2天的占比（复购率） | Python 从 MySQL 计算 | 2 天 |
> | ads_user_rfm | 近 30 天 RFM 三维 5 档打分 + 8 类标签 | Python 从 MySQL 计算 | 1865 行 |
>
> **建表 DDL 要点**：分区字段 dt 不能同时出现在列定义里，否则报 Column repeated in partitioning columns。
>
> ~~~sql
> CREATE TABLE ads_trade_stats(
>     gmv DECIMAL(16,2), order_count BIGINT,
>     pay_user_count BIGINT, avg_pay_amount DECIMAL(16,2)
> ) PARTITIONED BY (dt STRING) STORED AS ORC
> TBLPROPERTIES ('orc.compress'='SNAPPY');
> ~~~

### Q42: 复购率（repurchase_rate）是怎么计算的？

> **参考回答**：
> **定义**：当日下单用户中，历史累计在 ≥2 个不同日期下过单的用户占比（跨天复购）。
>
> **计算逻辑**（Python + MySQL）：
> 1. 取当天（dt）有下单的不同 user_id（total_amount>0）→ order_user_count
> 2. 对这些用户统计历史累计下单天数 COUNT(DISTINCT DATE(create_time)) WHERE dt<=当天
> 3. 天数 ≥2 的用户数 → repurchase_user_count
> 4. repurchase_rate = repurchase_user_count / order_user_count
>
> **本项目结果**：
> | 日期 | 当日下单用户 | 复购用户 | 复购率 |
> |---|---|---|---|
> | 2026-08-19 | 140 | 122 | 0.8714 |
> | 2026-08-20 | 128 | 109 | 0.8516 |
>
> **为什么用累计下单天数≥2 而不是订单数≥2**：同一天可能多单，按订单数几乎人人复购无意义；按不同天才能体现真正的回访复购。

### Q43: RFM 用户分层是怎么打分和分群的？

> **参考回答**：
> **RFM 模型**：用 3 个维度量化用户价值，时间窗口取近 30 天。
> - **R**（Recency）：最近一次消费距今天数，越小越好
> - **F**（Frequency）：30 天内消费天数，越大越好
> - **M**（Monetary）：30 天内消费总金额，越大越好
>
> **打分（5 档 NTILE）**：
> - F/M：按值升序 NTILE(5)，值大 → 分高（5 分）
> - R：按天数升序 NTILE(5) 后反转 6 - tile，天数小（最近消费）→ 5 分
>
> **8 类分群（≥3 为高）**：
> | R | F | M | 标签 |
> |---|---|---|---|
> | 高 | 高 | 高 | 重要价值用户 |
> | 低 | 高 | 高 | 重要保持用户 |
> | 高 | 低 | 高 | 重要发展用户 |
> | 低 | 低 | 高 | 重要挽留用户 |
> | 高 | 高 | 低 | 一般价值用户 |
> | 低 | 高 | 低 | 一般保持用户 |
> | 高 | 低 | 低 | 一般发展用户 |
> | 低 | 低 | 低 | 一般挽留用户 |
>
> **本项目结果**（2026-08-20，942 用户）：重要价值 327、重要保持 190、一般发展 179、一般挽留 150 等。

### Q44: ADS 数据怎么导出到 MySQL 给业务用？

> **参考回答**：
> ADS 在 Hive 里是 ORC 内部表，业务系统（BI/运营后台）直接读 Hive 不现实，所以导出到 MySQL mall_ads 库。
>
> **流程**（export_ads_to_mysql.py）：
> 1. 在 MySQL 建 mall_ads 库 + 3 张结果表（带主键 dt / dt+user_id）
> 2. hive -e SELECT ... FROM ads_xxx WHERE dt=... 查询，TSV 输出
> 3. Python 解析后 executemany 批量插入，用 ON DUPLICATE KEY UPDATE 幂等防重
>
> **为什么用主键 + ON DUPLICATE KEY UPDATE**：重跑导出时覆盖旧数据，保证幂等，不产生重复行。
>
> **结果**：trade_stats 2 行、repurchase_rate 2 行、user_rfm 1865 行，BI 直接 SELECT 即可出报表。

### Q45: ADS 复杂指标为什么用 Python 加工而不是纯 Hive SQL？

> **参考回答**：
> **背景**：ads_trade_stats（单表聚合）用 Hive 本地模式一句话搞定；但 ads_repurchase_rate（多表 JOIN，7 个 stage）和 ads_user_rfm（NTILE 窗口函数）在 Hive 本地模式会 OOM 失败（return code 2），YARN 模式又因 mapreduce.application.classpath 缺失报 Could not find MRAppMaster。
>
> **解决方案**：复杂 JOIN/窗口指标改用 Python 从 MySQL 读源数据，内存里用 defaultdict 聚合 + 手写 ntile5 分档，结果写 HDFS TextFile，再通过临时外部表 INSERT OVERWRITE 转 ORC 内部表（简单 SELECT 不触发多 stage，本地模式稳定）。
>
> **取舍**：
> - 优点：避开 Hive MR 多 stage 的内存/稳定性问题，Python 逻辑可读、可调试
> - 缺点：复杂指标走业务库而非 DWS，分层上不纯粹
>
> **面试话术**：生产环境这类复杂指标一般上 Spark/ClickHouse，本项目为控制组件规模用 Python 兜底，保证结果正确与流程完整。

---

## 十一、快速回答模板（背诵版）

| 问题 | 一句话回答 |
|---|---|
| Hive 本质是什么？ | 把 SQL 翻译成 MapReduce 在 HDFS 上执行的工具 |
| 数仓分哪几层？ | ODS（贴源）→ DWD（明细）→ DWS（汇总）→ ADS（应用） |
| DataX 做什么的？ | 异构数据源之间的离线数据同步工具 |
| 为什么用 Hive 不用 MySQL？ | 海量数据离线分析，列式存储比行存储快几十倍 |
| 分区的作用？ | 减少扫描范围，只处理相关目录的数据 |
| 外部表和内部表区别？ | 外部表删表不删 HDFS 数据，内部表一起删 |
| Metastore 用 MySQL 原因？ | Derby 不支持并发，MySQL 稳定可靠支持多会话 |
| Hadoop 三大组件？ | HDFS 存储 + YARN 调度 + MapReduce 计算 |
| 全量和增量同步？ | 全量每次同步所有数据，增量只同步变化的数据 |
| 为什么要造数？ | Demo 项目需要模拟真实业务数据，测试清洗逻辑 |
| 脏数据比例？ | 约 5%，包含空值、负值、非法时间 |
| SCD2 拉链表？ | 用 start_date/end_date 记录版本历史，支持回溯查询 |
| 什么是维度退化？ | 把维度属性冗余到事实表，减少下游 JOIN |
| ODS 层表格式？ | TextFile，可读性优先，后续转 ORC |
| 分区键选择？ | 事实表用 create_time，维度表用 operate_time |
| DWD 清洗规则？ | 空值填充、负值过滤、时间标准化、类型转换 |
| DWS 和 DWD 区别？ | DWD 是明细一行一笔，DWS 是汇总一行一用户/商品 |
| RFM 模型是什么？ | 用户价值分析：R最近消费+F消费频次+M消费金额 |
| GMV 怎么算？ | 所有订单金额总和，包括未支付 |
| 客单价怎么算？ | GMV / 订单数 |
| 支付率怎么算？ | 已支付订单数 / 总订单数 |
| ADS 层给谁用？ | 直接给 BI 报表和数据产品使用，开箱即用 |
| ORC 格式优点？ | 列式存储+高压缩，查询性能比 TextFile 快 5-10 倍 |
| 内部表和外部表怎么选？ | ODS 用外部表（易验证），DWS/ADS 用内部表（高性能） |
| Hive 本地模式？ | 小数据量查询不启动 MR，直接本地执行，速度提升 10 倍 |
| DWS 宽表怎么来的？ | Python 聚合 MySQL → HDFS TextFile → 外部表 → INSERT OVERWRITE 转 ORC |
| LOAD DATA 和 INSERT OVERWRITE 区别？ | LOAD DATA 直接导入，INSERT 触发 MR 转换格式 |
| 复购率怎么算？ | 当日下单用户中累计下单天数≥2 的占比 |
| RFM 怎么打分？ | R/F/M 各 NTILE(5) 分 5 档，R 反转，组合 8 类标签 |
| ADS 怎么导出 MySQL？ | hive -e 查 TSV → Python executemany + ON DUPLICATE KEY UPDATE 幂等 |
| ADS 复杂指标为何用 Python？ | Hive 多 stage JOIN/NTILE 本地模式 OOM，Python 兜底计算再 load ORC |
| 数仓怎么调度？ | cron 00:30 调 daily_scheduler.sh → run_pipeline.sh DAG 编排 8 步，flock 锁+JSONL 状态+2次退避重试 |
| 幂等怎么保证？ | state=SUCCESS 跳过、Hive INSERT OVERWRITE 覆盖分区、MySQL ON DUPLICATE KEY UPDATE |
| 失败怎么恢复？ | 自动重试2次+告警；修复后重跑脚本从失败任务续跑；--force 强制全量回补 |
| 为什么不用 Azkaban？ | WSL 资源不够（DS 要 ZK+多 Java 进程），Shell DAG 面试可讲白盒原理；保留平滑迁移方案 |
| 可视化用什么？ | Superset 直连 mall_ads，4 图表看板：GMV日报/趋势、复购率、RFM分群饼图 |
| 为何选 Superset？ | Apache 开源、Python 栈可 pip 装、40+ 数据源、单进程轻量（WSL 8G 能跑） |
| 看板口径谁定？ | ADS 层算好口径，看板只展示；BI 层轻、口径与调度产出 100% 一致 |
| Superset API 怎么调？ | login 拿 JWT → csrf_token 接口 → chart/data 带 Bearer+X-CSRFToken 拉数 |

---

## 十二、项目配置速查

### 核心端口号
| 服务 | 端口 |
|---|---|
| HDFS NameNode | 9000 |
| Hadoop ResourceManager | 8040 |
| HiveServer2 | 10000 |
| MySQL | 3306 |

### 核心命令
```bash
# 启动 Hadoop
start-dfs.sh && start-yarn.sh

# 启动 HiveServer2
$HIVE_HOME/bin/hiveserver2

# Hive 执行 SQL
$HIVE_HOME/bin/hive -f script.sql

# HDFS 操作
hdfs dfs -ls /warehouse/mall
hdfs dfs -put file.csv /warehouse/mall/table_name/

# 查看 Java 进程
jps

# 造数脚本
python3 /opt/script/mall/mock/mock_data.py init 30   # 初始化30天历史数据
python3 /opt/script/mall/mock/mock_data.py day        # 生成当日增量数据

# ODS 同步（替代 DataX）
python3 /opt/script/mall/datax/etl_sync.py sync <table> <mode> <dt>
python3 /opt/script/mall/datax/batch_sync.py 2026-07-21 2026-08-19

# DWD 加工
python3 /opt/script/mall/dwd/dwd_etl.py <dt> --init   # 首日全量
python3 /opt/script/mall/dwd/dwd_etl.py <dt>          # 每日增量

# DWS+ADS 加工
python3 /opt/script/mall/dws_ads/dws_ads_etl.py <dt>
```

### 核心配置文件路径
| 文件 | 路径 |
|---|---|
| Hadoop core-site.xml | /opt/module/hadoop-3.3.6/etc/hadoop/core-site.xml |
| Hive hive-site.xml | /opt/module/apache-hive-3.1.3-bin/conf/hive-site.xml |
| JDK 安装路径 | /usr/lib/jvm/java-8-openjdk-amd64 |
| Hadoop 安装路径 | /opt/module/hadoop-3.3.6 |
| Hive 安装路径 | /opt/module/apache-hive-3.1.3-bin |

### 项目脚本路径
| 脚本 | 用途 |
|---|---|
| mock_data.py | 造数脚本（全量/增量） |
| etl_sync.py | ODS 同步脚本（单表） |
| batch_sync.py | ODS 批量同步脚本（多日） |
| dwd_etl.py | DWD 加工脚本（全量/增量） |
| dws_ads_etl.py | DWS+ADS 外部表加工脚本 |
| prepare_dws_data.py | DWS 数据准备（MySQL 聚合到 HDFS） |
| load_dws.py | DWS 数据加载（HDFS TextFile → ORC 内部表） |
| process_ads.py | ADS 指标加工（Hive SQL，ads_trade_stats） |
| ads_etl_py.py | ADS 复杂指标加工（Python，repurchase + rfm） |
| export_ads_to_mysql.py | ADS 结果导出 Hive → MySQL mall_ads |
| create_ads_tables.sql | ADS 指标层建表（ORC 内部表） |
| scheduler_lib.sh | 调度工具库（日志/flock 锁/JSONL 状态/退避重试/告警） |
| run_pipeline.sh | 单天 DAG 编排入口（造数→ODS→DWD→DWS→ADS→MySQL） |
| daily_scheduler.sh | 每日调度触发器（cron 调它跑 T-1） |
| install_cron.sh | 自动把 daily_scheduler 装到 crontab 00:30 |
| mail_alert.sh | 邮件告警脚本（SMTP SSL 465，未配置自动降级日志告警） |
| setup_superset_datasource.py | Superset 自动化配置（数据源+数据集+图表+看板 ORM 创建） |
| verify_superset.py | Superset API 全链路验证（登录→看板→图表数据拉取） |

### 调度目录与状态路径
| 类型 | 路径 |
|---|---|
| 调度脚本 | /opt/script/mall/scheduler/ |
| 调度日志 | /data/logs/scheduler/<dt>/<task>.log |
| 任务状态 JSONL | /data/scheduler/state/<dt>/<task>.state.jsonl |
| 任务最新状态 | /data/scheduler/state/<dt>/<task>.state.latest.json |
| 全局 DAG 锁 | /data/scheduler/lock/pipeline_mall_YYYYMMDD.lock |
| 调度 cron 条目 | 30 0 * * * /bin/bash /opt/script/mall/scheduler/daily_scheduler.sh 1 |
| Superset venv | /opt/module/superset_env/（superset_config.py + superset.db） |
| Superset 访问 | http://localhost:8088 （admin / admin123），看板 /superset/dashboard/mall_dw/ |

### HDFS 数据路径
| 层级 | 路径 |
|---|---|
| ODS 贴源层 | /warehouse/mall/ods/{table_name}/dt={date} |
| DWD 明细层 | /warehouse/mall/dwd/{table_name}/dt={date} |
| DWS 汇总层（外部表） | /warehouse/mall/dws/{table_name}/dt={date} |
| DWS 汇总层（内部表） | /warehouse/mall.db/{table_name}/dt={date} |
| ADS 应用层 | /warehouse/mall/ads/{table_name} |

---

> **面试提示**：重点掌握 **项目架构图 → 数仓分层思路 → 踩坑经验** 三个维度，回答时结合本项目的实际配置和数据量（1120 用户/200 商品/6500+ 订单）举例说明，会显得更加真实可信。

> **各阶段核心话术**：
> - **造数**："我们用 Python 造数脚本模拟电商业务，生成 30 天历史数据，注入 5% 脏数据测试清洗逻辑，随机变更老用户手机号驱动 SCD2 拉链表。"
> - **ODS 同步**："用 Python 替代 DataX 实现 MySQL→HDFS 同步，小表全量大表增量，事实表按 create_time 分区，外部表方式存储。"
> - **DWD 加工**："清洗空值/负值/非法时间，维度退化将用户/商品属性冗余到订单宽表，SCD2 拉链表保留用户变更历史。"
> - **DWS 外部表**："用 Python 聚合 MySQL 数据到 HDFS，创建 TextFile 外部表，快速验证聚合结果正确性。"
> - **DWS ORC 内部表**："将外部表数据 INSERT OVERWRITE 到 ORC 内部表，开启本地模式加速小数据量转换，Snappy 压缩节省 50% 存储空间。"
> - **ADS 应用层**："ADS 层做 RFM 用户分层、GMV 日报、销售排行榜，直接供 BI 使用。"
> - **可视化看板**："Superset 直连 mall_ads 建 4 图表看板（GMV 日报大数字+趋势折线、复购率、RFM 饼图），全程 ORM API 自动化创建；JWT+CSRF 双认证调 chart/data API 验证数据口径。"
> - **调度与告警**："用 cron + Shell DAG 做每日 T-1 全链路调度（造数→ODS→DWD→DWS→ADS→MySQL），指数退避重试 2 次，状态 JSONL 落盘可监控，失败自动邮件告警；生产可换 Azkaban/DolphinScheduler。"

---

## 十一、调度与告警（阶段八）

### Q46: 本项目的调度方案是怎样的？为什么选 cron+Shell 而不是 Azkaban/DolphinScheduler？

> **参考回答**：
> 本项目按"生产思想 + demo 资源约束"分层：
> 1. **每日触发器**：Linux crontab 每天 00:30 调用 `daily_scheduler.sh 1`（`1` 代表 T-1）。
> 2. **DAG 调度器**：`run_pipeline.sh <dt>` 编排 8 步串行（造数→ODS→DWD→DWS prepare→DWS ORC load→ADS trade→ADS complex→ADS→MySQL），`dag_step()` 统一包装"幂等校验 → 指数退避重试(10s/30s/60s) → 状态落盘 → 失败告警"。
> 3. **基础设施库**：`scheduler_lib.sh` 提供日志、`flock` 分布式锁、JSONL 状态、重试、告警 5 类能力。
>
> **选型原因**：
> - WSL 单节点 8G 内存，跑 HDFS/YARN/Hive 已经占满，DS 需要 ZK + 多 Java 服务装不下。
> - cron+Shell 零依赖，面试白板可直接讲清 DAG 执行顺序与失败处理，比 Azkaban Web UI 截图更能体现理解。
> - 保留平滑升级路径：把 `dag_step()` 换成 Azkaban job 类型，`run_pipeline.sh` 即可作为单个 Flow 的 command。

### Q47: 怎么保证调度的幂等性？重跑一天数据会重复吗？

> **参考回答**：
> **三层幂等设计**：
> 1. **状态机层**：每个任务 dt 维度写 `state.latest.json`，成功就写 SUCCESS，下次 `skip_if_success()` 直接跳过。`--force` 时清 state 强制重跑。
> 2. **数据写入层**：
>    - Hive 用 `INSERT OVERWRITE TABLE partition(dt='...')`，覆盖写不是 append，同一 dt 重跑结果一致。
>    - MySQL 用 `INSERT ... ON DUPLICATE KEY UPDATE`，以 `dt` 或 `(dt,user_id)` 为主键，重跑不会重复行。
>    - ODS 层 `hdfs dfs -test -e && hdfs dfs -rm -r` 先删后传，保证分区内只有一份文件。
> 3. **进程锁层**：`acquire_lock(pipeline_mall_YYYYMMDD)` 用 `flock` 锁同一 dt，防止 cron 重复触发 / 人工与系统同时调度。
>
> 实测：2026-08-21 用 `--force` 连跑 2 次，mall_ads 主键冲突自动 UPDATE，ADS 行数 GMV 等数值完全一致。

### Q48: 任务失败了怎么处理？怎么重试、怎么告警、怎么恢复？

> **参考回答**：
> **失败处理链路**（本项目 08_ads_export_mysql 实际踩过的案例）：
> 1. **自动重试**：`run_with_retry()` 每任务最多 2 次重试，退避 10s → 30s → 60s；每次状态分别记 RUNNING / RETRY / FAILED。
> 2. **告警推送**：失败后 `send_alert(CRITICAL)` 调 `mail_alert.sh`（用 `sendemail` + SMTP SSL 465 端口）；未配置邮箱时自动降级为日志告警，不阻塞流程。
> 3. **人工介入**：查 `/data/logs/scheduler/<dt>/<task>.log` 看 stderr 与 rc；修复 bug 后：
>    - 方案 A（推荐）：直接跑 `run_pipeline.sh <dt>`，前 7 步 state=SUCCESS 全部跳过，从第 8 步失败点续跑。
>    - 方案 B（全量回补）：加 `--force` 清所有 state 从头跑，INSERT OVERWRITE + ON DUPLICATE KEY 保证幂等。
>
> **实际案例**：export_ads_to_mysql.py 因 `hive -e` 用 bash -c 传参且命令里有中文路径，导致 hive 查询空结果 → `r[0]` IndexError。修复为写临时 SQL 文件 + `hive -f`，第 8 步直接续跑 1 次成功。

### Q49: DAG 的依赖关系怎么表达？有哪些调度优化？

> **参考回答**：
> **DAG 拓扑（本项目）**：
> ```
> mock → ods_sync → dwd_etl → dws_prepare → dws_load_orc
>                                             ├─→ ads_trade_stats ─┐
>                                             └─→ ads_complex_py ──┴─→ ads_to_mysql → verify
> ```
> 用 bash 函数顺序表达串行依赖；ADS 两个子任务相互独立可以并行（后台 `&` + `wait`），考虑演示可读性先串行。
>
> **调度优化**：
> 1. **错峰调度窗口**：每日 00:30 调度 T-1，避开 00:00 业务系统日切。
> 2. **上游必检**：ODS 前检查 MySQL alive / HDFS safemode=OFF / Hive Metastore 可连，三缺一则提前报错不浪费重试配额。
> 3. **按天分区并发**：多日期回补时可起多个 `run_pipeline.sh <dt>` 实例，flock 按 dt 粒度互不阻塞。
> 4. **输出阈值校验**：最后一步 verify 检查行数，GMV 为 0 或 RFM 行数异常走 WARNING 级别告警，不挂 DAG。

### Q50: 如果要升级到 Azkaban/DolphinScheduler，怎么迁移？

> **参考回答**：
> 本项目的 Shell DAG 就是 Azkaban Command Job / DS Shell 节点的天然等价物，迁移三步走：
> 1. **拆分 job**：把每个 `dag_step xxx "python3 ..."` 拆成 Azkaban `.job` 单文件或 DS 的一个 Shell Task。
> 2. **DAG 依赖表达**：
>    - Azkaban：用 `dependencies=job1,job2` 声明。
>    - DolphinScheduler：Web 画布拖拽连线，定义串行与并行分支（ADS 的两个子任务天然可并行 + 依赖汇合节点）。
> 3. **公共能力对接**：
>    - 状态落盘：继续复用 JSONL，新增 DS/Azkaban 回调同步到外部监控。
>    - 告警：把 `mail_alert.sh` 替换为 DS 的告警实例组（邮件+钉钉/企业微信），配置失败/超时/告警抑制策略。
>    - 调度时间：把 crontab 的 `30 0 * * *` 复制到 Azkaban Flow Schedule / DS Cron。
> 4. **回跑能力**：利用 DS "补数"功能按日期范围批量触发，替代 `for dt in ... ; do run_pipeline.sh $dt; done` 脚本循环。

---

## 十一（续）、可视化看板（阶段九）

### Q51: 报表可视化是怎么做的？为什么选 Superset？

> **参考回答**：
> **架构**：ADS 层数据每日导出到 MySQL `mall_ads` 库，Superset 直连 MySQL 做可视化，形成"数仓加工 → MySQL → BI 看板"的完整链路。
>
> **部署方式**（WSL 单节点，Python venv 隔离）：
> ```
> /opt/module/superset_env/          # venv（Flask 3.1 + SQLAlchemy 1.4）
> ├── superset_config.py             # SECRET_KEY + SQLite 元数据库
> └── superset.db                    # Superset 自身元数据（数据集/图表/看板定义）
> 启动: superset run -p 8088 --host 0.0.0.0
> 访问: http://localhost:8088  (admin / admin123)
> ```
>
> **选型理由**：
> - 开源免费（Apache 顶级项目），对比 Tableau/PowerBI 无 License 成本
> - Python 技术栈，与数仓 ETL 工具链一致，可 pip 安装、可 API 自动化建看板
> - 原生支持 40+ 数据源（MySQL/Hive/ClickHouse 都能连），后期切 OLAP 引擎不用换 BI
> - 轻量：单进程即可跑，WSL 8G 内存装得下（DolphinScheduler 装不下的机器跑 Superset 没问题）

### Q52: 看板上有哪些指标？怎么设计的？

> **参考回答**：
> 看板名"电商离线数仓看板"（slug=mall_dw），4 张图表对应 3 个数据集：
>
> | 图表 | 可视化类型 | 数据集 | 核心指标 |
> |---|---|---|---|
> | GMV日报 | big_number 大数字 | ads_trade_stats | SUM(gmv) 按日 |
> | GMV订单趋势 | line 折线图 | ads_trade_stats | GMV总额 + 订单数 双指标 |
> | 复购率趋势 | line 折线图 | ads_repurchase_rate | AVG(复购率) + SUM(下单用户数) |
> | RFM用户分群 | pie 饼图 | ads_user_rfm | COUNT(DISTINCT user_id) GROUP BY rfm_label |
>
> **实际数据验证**（API 拉取）：
> - GMV 3 天趋势：591020.68 → 456966.77 → 410354.42
> - RFM 8 类分群：重要价值用户 378 / 重要保持 236 / 一般发展 229 / 一般挽留 185 / 一般价值 57 / 重要发展 39 / 重要挽留 32 / 一般保持 12
>
> **设计原则**：指标口径全部下沉到 ADS 层算好，看板只做展示不做复杂计算——保证口径与调度产出的 ADS 数据 100% 一致，BI 层永远轻。

### Q53: Superset 的建看板过程怎么自动化？踩过什么坑？

> **参考回答**：
> 没有手工点 Web UI，写 Python 脚本（setup_superset_datasource.py）用 Superset ORM 模型一次创建：
> 1. `Database` 对象：mysql+pymysql://root:***@localhost:3306/mall_ads
> 2. `SqlaTable` 数据集 ×3：`fetch_metadata()` 自动同步列信息
> 3. `Slice` 图表 ×4：viz_type + params（JSON 格式的 metrics/groupby）
> 4. `Dashboard`：slices 关联 + position_json 布局（GRID/ROW/CHART 三层结构）
>
> **踩坑记录**（面试亮点）：
> - **坑1** `SqlaTable.sync_from_source()` 新版改名 → `fetch_metadata()`
> - **坑2** pip 装 superset 缺 `rich`、`cachetools` 两个 CLI 依赖，需补装
> - **坑3** SQLAlchemy URI 用 `mysql+pymysql://` 但 engine spec 内部仍调 MySQLdb → `pip install mysqlclient`（编译装，需 default-libmysqlclient-dev + python3-dev）
> - **坑4** WSL 退出会杀掉 nohup 后台进程 → Superset 必须在独立长驻会话前台跑，或用 systemd/service 托管
> - **坑5** chart/data API 的 `datasource` 必须放 payload 顶层（不是 queries 里），且 `time_range` 格式要求 "100 years ago : now" 这种明确方向写法

### Q54: Superset 的 API 调用流程是怎样的？（认证体系）

> **参考回答**：
> Superset 用 JWT Bearer + CSRF 双重认证：
> ```
> 1. POST /api/v1/security/login  {"username","password","provider":"db"}
>    → 返回 access_token (JWT)
> 2. GET  /api/v1/security/csrf_token/  (带 Bearer header)
>    → 返回 CSRF token（写操作必须）
> 3. POST /api/v1/chart/data
>    Headers: Authorization: Bearer <jwt> + X-CSRFToken: <token>
>    Body: {"datasource": {"id":1,"type":"table"}, "queries": [{groupby, metrics, ...}]}
>    → 返回图表数据
> ```
> 这套流程的意义：外部系统（如定时报表推送机器人）可以免登录调 Superset API 拉数，实现"看板数据自动推送钉钉/邮件"的进阶玩法。

（完）


