# 项目一 电商离线数仓 成果查收操作文档

## 文档信息

- **项目名称**：电商离线数据仓库（MySQL 业务库 → 分层加工 → ADS 指标 → BI 看板）
- **环境**：Windows 11 + WSL2 Ubuntu-22.04（单机伪分布式）
- **技术栈**：Hadoop 3.3.6 + Hive 3.1.3 + MySQL 8.0 + Python 3.10 + Apache Superset + Shell
- **文档用途**：成果查收。按"启动环境 → 逐层查收 → 关闭环境"顺序操作，每步给出命令与预期结果
- **当前状态**：九个阶段全部完成，所有服务已停止，WSL 已关闭

---

## 一、成果总览

| 阶段 | 内容 | 核心交付物 | 查收方式 |
|---|---|---|---|
| 阶段一 | 环境搭建 | Hadoop 伪分布式 + Hive + MySQL 全套 | §3.1 jps 进程检查 |
| 阶段二 | 业务造数 | mall 库 5 张业务表 + 30 天历史 + 增量/脏数据 | §3.2 |
| 阶段三 | ODS 同步 | Python ETL 全量/增量同步到 HDFS + Hive 外部表 | §3.3 |
| 阶段四 | DWD 清洗 | 脱敏/空值/负值过滤/维度退化/SCD2 拉链 | §3.4 |
| 阶段五 | DWS+ADS 外部表版 | TEXTFILE 快速验证链路 | §3.5 |
| 阶段六 | DWS ORC 内部表 | SNAPPY 压缩列存，prepare+load 两段式装载 | §3.5 |
| 阶段七 | ADS 指标层 | GMV/复购率/RFM 三大指标 + 导出 mall_ads | §3.6 |
| 阶段八 | 调度告警 | Shell DAG 8 步 + flock 锁 + 重试 + cron | §3.7 |
| 阶段九 | 可视化看板 | Superset 4 图表看板，API 验证通过 | §3.8 |

**数据流全链路**：

```
Python 造数器 ──> MySQL 业务库(mall)
                      │
                      ├── Python ETL(全量/增量) ──> HDFS ODS 贴源层
                      │                                │
                      │                     Hive SQL 清洗(脱敏/去脏/维度退化)
                      │                                │
                      │                              DWD 明细层
                      │                                │
                      │                  Python 聚合 + ORC 装载(两段式)
                      │                                │
                      │                          DWS 汇总层(ORC+SNAPPY)
                      │                                │
                      │                  Hive SQL + Python 复杂指标(RFM)
                      │                                │
                      │                          ADS 指标层(ORC)
                      │                                │
                      └────────── 导出(幂等) <──────┘
                                                     │
                                             MySQL 指标库(mall_ads)
                                                     │
                                            Superset BI 看板(4 图表)

贯穿：Shell DAG 调度(cron 00:30 触发 T-1) + flock 锁 + 指数退避重试 + 邮件告警
```

---

## 二、环境与凭据速查

### 2.1 关键路径

| 位置 | 路径 | 说明 |
|---|---|---|
| Hadoop | `/opt/module/hadoop-3.3.6` | HDFS + YARN |
| Hive | `/opt/module/apache-hive-3.1.3-bin` | 含 metastore |
| Superset venv | `/opt/module/superset_env` | 含 superset_config.py + superset.db |
| 项目脚本(WSL) | `/opt/script/mall/` | 全部脚本部署目录 |
| 调度脚本(WSL) | `/opt/script/mall/scheduler/` | DAG + 调度库 + 告警 |
| HDFS 数仓根 | `/warehouse/mall/` | 各层数据目录 |
| 调度状态 | `/data/scheduler/state/<dt>/` | JSONL 步骤状态 |
| 运行日志 | `/data/logs/scheduler/`、`/data/logs/superset/` | 调度/看板日志 |
| 项目脚本(Win) | `E:\简历项目\01-离线数仓项目\` | 源文件（与 WSL 侧同步） |

### 2.2 凭据

| 服务 | 账号/密码 | 访问方式 |
|---|---|---|
| MySQL | root / 123456 | WSL 内 `mysql -uroot -p123456` |
| Superset | admin / admin123 | http://localhost:8088 |
| Hive | 免密 | `hive` CLI（依赖 metastore 启动） |

---

## 三、查收操作

### 3.0 启动环境（查收前提）

打开 Windows 终端，进入 WSL：

```bash
wsl -d Ubuntu-22.04
```

按依赖顺序启动（每次查收按需启动对应层级）：

```bash
# 1. MySQL（Hive metastore 和 Superset 都依赖它）
sudo service mysql start

# 2. Hadoop（查收 HDFS/Hive 数据时需要）
source /etc/profile; source ~/.profile
start-dfs.sh && start-yarn.sh

# 3. Hive Metastore（查收 Hive 表时需要，后台运行）
nohup hive --service metastore > /tmp/metastore.log 2>&1 &
# 等待 10 秒左右再执行 hive 命令
```

> **注意**：Superset 不能用 nohup 后台跑（WSL 会话退出会杀进程），查收看板时见 §3.8 的启动方式。

### 3.1 阶段一：环境搭建查收

```bash
jps
```

**预期输出**（5 个进程）：

```
xxxxx NameNode
xxxxx DataNode
xxxxx SecondaryNameNode
xxxxx ResourceManager
xxxxx NodeManager
```

### 3.2 阶段二：业务造数查收

```bash
mysql -uroot -p123456 mall -e "
SELECT '用户' t, COUNT(*) cnt FROM user_info
UNION ALL SELECT '商品', COUNT(*) FROM sku_info
UNION ALL SELECT '订单', COUNT(*) FROM order_info
UNION ALL SELECT '订单明细', COUNT(*) FROM order_detail
UNION ALL SELECT '支付', COUNT(*) FROM payment_info;"
```

**预期输出**：

| t | cnt |
|---|---|
| 用户 | 1120 |
| 商品 | 200 |
| 订单 | 6592+（含后续增量） |
| 订单明细 | 16468+ |
| 支付 | 4450+ |

**脏数据设计查收**（面试讲解点，故意造的）：

```bash
mysql -uroot -p123456 mall -e "
SELECT COUNT(*) 空手机号 FROM user_info WHERE phone IS NULL OR phone='';
SELECT COUNT(*) 负金额订单 FROM order_info WHERE total_amount < 0;"
```

预期：空手机号约 24 条、负金额订单约 113 条（DWD 清洗的输入素材）。

### 3.3 阶段三：ODS 同步查收

```bash
# HDFS 侧
hdfs dfs -ls /warehouse/mall/ | head -20

# Hive 侧
hive -e "USE mall; SHOW TABLES;" 2>/dev/null | grep ods
```

**预期**：HDFS 有各业务表目录（按 dt 分区子目录）；Hive 有 ods_ 前缀外部表。

```bash
# 抽查一天数据
hive -e "USE mall; SELECT COUNT(*) FROM ods_order_info WHERE dt='2026-08-20';" 2>/dev/null
```

### 3.4 阶段四：DWD 清洗查收

```bash
hive -e "
USE mall;
SHOW TABLES;" 2>/dev/null | grep -E 'dim_|fact_'
```

**预期**：`dim_user_full`（SCD2 拉链）、`dim_sku_full`、`fact_order_detail`、`fact_payment_detail`。

**清洗效果查收**（面试核心讲解点）：

```bash
hive -e "
USE mall;
-- 1. 手机号脱敏：151****6474 格式
SELECT user_phone FROM fact_order_detail LIMIT 3;
-- 2. 负金额订单已过滤（对比业务库 113 条）
SELECT COUNT(*) FROM fact_order_detail WHERE order_amount < 0;
-- 3. 维度退化：事实表冗余了用户昵称/商品名/品类名
SELECT user_nick_name, sku_name, category3_name FROM fact_order_detail LIMIT 3;" 2>/dev/null
```

**预期**：手机号为掩码格式；负金额计数为 0；退化维度字段有值。

**数据量参考**：fact_order_detail 全量 1816 行（2026-08-19）+ 增量 641 行（2026-08-20）+ 后续调度日数据。

### 3.5 阶段五/六：DWS 汇总层查收

```bash
hive -e "
USE mall;
-- 阶段六 ORC 内部表（正式版）
DESC FORMATTED dws_trade_user_order_1d;" 2>/dev/null | grep -E 'Table Type|InputFormat|Compress'
```

**预期**：

```
Table Type:       MANAGED_TABLE          -- 内部表
InputFormat:      org.apache.hadoop.hive.ql.io.orc.OrcInputFormat
Compress:         SNAPPY                 -- 或 compression:SNAPPY
```

```bash
# 各日行数
hive -e "
USE mall;
SELECT dt, COUNT(*) FROM dws_trade_user_order_1d GROUP BY dt;
SELECT dt, COUNT(*) FROM dws_trade_sku_order_1d GROUP BY dt;" 2>/dev/null
```

**预期参考**：

| dt | 用户宽表 | 商品宽表 |
|---|---|---|
| 2026-08-19 | 140 | 169 |
| 2026-08-20 | 128 | 162 |
| 2026-08-21 | （调度产出） | （调度产出） |

### 3.6 阶段七：ADS 指标层查收（重点）

ADS 已导出到 MySQL `mall_ads` 库，**无需启动 Hadoop，只要 MySQL 在跑即可查收**：

```bash
mysql -uroot -p123456 mall_ads -e "
-- 1. GMV 日报（核心指标）
SELECT * FROM ads_trade_stats ORDER BY dt;
-- 2. 复购率
SELECT * FROM ads_repurchase_rate ORDER BY dt;
-- 3. RFM 分群
SELECT rfm_label, COUNT(*) 人数 FROM ads_user_rfm WHERE dt='2026-08-21' GROUP BY rfm_label ORDER BY 人数 DESC;"
```

**预期输出**：

ads_trade_stats（3 天）：

| dt | gmv | order_count | pay_user_count | avg_pay_amount |
|---|---|---|---|---|
| 2026-08-19 | 591020.68 | ... | ... | ... |
| 2026-08-20 | 456966.77 | ... | ... | ... |
| 2026-08-21 | 410354.42 | 113 | 77 | 3631.46 |

ads_repurchase_rate（2026-08-21）：下单用户 110 / 复购用户 93 / **复购率 0.8455**

ads_user_rfm 8 类分群（2026-08-21）：

| rfm_label | 人数 |
|---|---|
| 重要价值用户 | 378 |
| 重要保持用户 | 236 |
| 一般发展用户 | 229 |
| 一般挽留用户 | 185 |
| 一般价值用户 | 57 |
| 重要发展用户 | 39 |
| 重要挽留用户 | 32 |
| 一般保持用户 | 12 |

> RFM 总行数 2824（3 天累计）。口径：R/F/M 各 5 档（NTILE 等价分桶），组合出 8 类运营标签。

Hive 侧同结构 ORC 表查收（需 Hadoop）：

```bash
hive -e "USE mall; SELECT * FROM ads_trade_stats ORDER BY dt;" 2>/dev/null
```

### 3.7 阶段八：调度系统查收

```bash
# 1. cron 条目（每天 00:30 跑 T-1）
crontab -l | grep daily_scheduler
```

**预期**：

```
30 0 * * * /bin/bash /opt/script/mall/scheduler/daily_scheduler.sh 1 >> /data/logs/scheduler/cron.log 2>&1
```

```bash
# 2. 最近一次 DAG 运行状态（8 步 JSONL）
cat /data/scheduler/state/2026-08-21/*.jsonl | python3 -m json.tool --json-lines 2>/dev/null \
  || cat /data/scheduler/state/2026-08-21/*.jsonl

# 3. 调度日志
ls /data/logs/scheduler/2026-08-21/
```

**预期**：状态文件里 9 个步骤记录（01_mock ~ 09_verify），状态为 SUCCESS/SKIPPED。

**DAG 拓扑**（8 步串行 + 分叉）：

```
mock → ods_sync → dwd_etl → dws_prepare → dws_load_orc
                                    ├─→ ads_trade_stats ─┐
                                    └─→ ads_complex_py ──┴─→ ads_to_mysql → verify
```

**手工补跑单日**（演示用）：

```bash
# 完整跑一天（含造数）
bash /opt/script/mall/scheduler/run_pipeline.sh 2026-08-22

# 只重跑加工不造数（业务库已有当天数据）
bash /opt/script/mall/scheduler/run_pipeline.sh 2026-08-22 --skip-mock

# 强制重跑（忽略"已完成"状态）
bash /opt/script/mall/scheduler/run_pipeline.sh 2026-08-22 --skip-mock --force
```

**调度能力清单**（面试讲解点）：
- `flock` dt 粒度锁：同一天不会并发重入
- 指数退避重试：10s / 30s / 60s 三档
- 状态机：NEW → RUNNING → SUCCESS/FAILED/RETRY/SKIPPED
- 幂等三层保障：状态文件 + INSERT OVERWRITE + MySQL ON DUPLICATE KEY UPDATE
- 告警：失败自动调 mail_alert.sh（SMTP 未配置时降级日志告警）

### 3.8 阶段九：Superset 看板查收

Superset 需在**独立长驻终端**前台运行（WSL 特性：会话退出杀后台进程）。

**方式一**：开一个专用 WSL 终端窗口，保持不关：

```bash
wsl -d Ubuntu-22.04
sudo service mysql start
source /opt/module/superset_env/bin/activate
export SUPERSET_CONFIG_PATH=/opt/module/superset_env/superset_config.py
superset run -p 8088 --host 0.0.0.0 --with-threads
```

**方式二**（推荐日常用）：Windows 直接开个终端跑上面的命令，查完 Ctrl+C 停掉。

启动后 Windows 浏览器访问：

```
http://localhost:8088
账号: admin
密码: admin123
```

**查收路径**：Dashboards → **电商离线数仓看板**（slug=mall_dw）

**预期 4 张图表**：

| 图表 | 类型 | 数据来源 | 验证点 |
|---|---|---|---|
| GMV日报 | 大数字 | ads_trade_stats | 显示最近日期 GMV |
| GMV订单趋势 | 折线图 | ads_trade_stats | 3 天曲线 591020→456966→410354 |
| 复购率趋势 | 折线图 | ads_repurchase_rate | 双指标曲线 |
| RFM用户分群 | 饼图 | ads_user_rfm | 8 类扇区，重要价值用户最大 |

**API 自动化验证**（不开浏览器也能查收）：

```bash
source /opt/module/superset_env/bin/activate
python3 /mnt/e/简历项目/01-离线数仓项目/verify_superset.py
```

预期输出 `[1]~[6]` 六步全过：JWT 登录 → CSRF → 看板列表 → 图表列表 → GMV 3 行数据 → RFM 8 类人数。

### 3.9 关闭环境

```bash
# 1. Superset：在其终端 Ctrl+C

# 2. 停 Hadoop
stop-yarn.sh && stop-dfs.sh

# 3. 停 MySQL
sudo service mysql stop

# 4. 确认无残留
jps   # 应只剩 Jps 自身

# 5. Windows PowerShell 关 WSL
wsl -t Ubuntu-22.04
wsl -l -v   # 确认 STATE = Stopped
```

---

## 四、文件清单

### 4.1 Windows 侧核心脚本（E:\简历项目\01-离线数仓项目\）

**按阶段划分**：

| 阶段 | 脚本 | 用途 |
|---|---|---|
| 二 | `mock_data.py` | 造数器（init 全量 / day 增量） |
| 二 | `fix_sku_table.py` | 修复 sku_id 范围（1-200 对齐订单明细） |
| 三 | `etl_sync.py`、`batch_sync.py`、`datax_sync.py` | MySQL→HDFS 同步 |
| 四 | `create_dwd_tables.sql`、`dwd_etl.py` | DWD 建表 + 清洗加工 |
| 五 | `create_dws_ads_tables.sql`、`dws_ads_etl.py` | DWS/ADS 外部表版（快速验证） |
| 六 | `create_dws_orc_tables.sql`、`prepare_dws_data.py`、`load_dws.py` | ORC 内部表 + 两段式装载 |
| 七 | `create_ads_tables.sql`、`process_ads.py`、`ads_etl_py.py`、`export_ads_to_mysql.py` | ADS 建表/加工/导出 |
| 八 | `scheduler/` 目录（见 4.2） | 调度全家桶 |
| 九 | `setup_superset_datasource.py`、`verify_superset.py` | 看板自动化 + API 验证 |
| 通用 | `start_services.sh` | 一键启动脚本 |

**配套文档**：
- `离线数仓项目面试问答手册.md` — **Q1~Q54 共 54 问** + 背诵模板 + 配置速查 + 9 条阶段话术（约 1025 行）
- 本文档

### 4.2 调度目录（scheduler\）

| 文件 | 用途 |
|---|---|
| `scheduler_lib.sh` | 核心库：日志/flock 锁/JSONL 状态/重试/告警派发 |
| `run_pipeline.sh` | 单日 DAG 入口（支持 --skip-mock / --force） |
| `daily_scheduler.sh` | cron 触发器（自动算 T-1） |
| `mail_alert.sh` | 邮件告警（SMTP SSL 465，未配置时降级日志） |
| `install_cron.sh` | 幂等安装 cron 条目 |
| `deploy_to_wsl.sh` | 部署脚本到 WSL /opt/script/mall/ |
| `x_*.sh`（30 余个） | **调试临时脚本，可删除**（排障过程留存） |

### 4.3 WSL 侧部署位置

```
/opt/script/mall/            # 主脚本（deploy_to_wsl.sh 部署）
/opt/script/mall/scheduler/  # 调度脚本
/warehouse/mall/             # HDFS 数仓数据（随 vhdx 持久化）
/data/scheduler/state/       # 调度状态（按 dt 分目录）
/data/logs/                  # 全部运行日志
/opt/module/superset_env/    # Superset venv + 配置 + 元数据库
```

---

## 五、数据成果快照（查收基准）

| 指标 | 数值 |
|---|---|
| 业务库覆盖天数 | 2026-07-21 ~ 2026-08-21（30+ 天） |
| 用户数 | 1120 |
| 商品数 | 200 |
| 订单数 | 6592+（含增量） |
| 订单明细 | 16468+ |
| 支付记录 | 4450+ |
| 埋点日志 | 150000 |
| GMV（3 天） | 591020.68 / 456966.77 / 410354.42 |
| 复购率（8-21） | 0.8455（110 下单用户 / 93 复购） |
| RFM 分层 | 8 类 959 人（单日），累计 2824 行 |
| DWS 宽表 | 用户宽表 + 商品宽表，ORC+SNAPPY |
| 调度 | 9 步 DAG，cron 00:30，幂等可重跑 |
| 看板 | 4 图表，API 验证通过 |

---

## 六、遗留事项（不影响成果完整性）

1. **邮件告警为占位配置**：`scheduler/mail_alert.sh` 中 SMTP_HOST/USER/PASS/MAIL_TO 四项 TODO 未填真实邮箱，失败告警当前降级为日志输出。填入 QQ/163 邮箱授权码即可启用真实发信。
2. **cron 未实战触发**：条目已安装（00:30 跑 T-1），但 WSL 常关机属正常未触发。下次开机后可 `bash /opt/script/mall/scheduler/daily_scheduler.sh 1` 手动验证等价逻辑。
3. **x_*.sh 调试脚本**：scheduler 目录下 30 余个排障临时脚本，查收确认后可清理。

---

## 七、面试演示动线（10 分钟版）

1. **架构图**（手册 Q1）：MySQL → ETL → ODS → DWD → DWS → ADS → mall_ads → Superset
2. **造数**：`mock_data.py` init/day 双模式 + 脏数据设计（空值/负金额 → 清洗素材）
3. **DWD 清洗**：脱敏 + 负值过滤 + 维度退化 + SCD2（手册 Q17-Q24 区段）
4. **DWS ORC**：两段式装载 + TEXTFILE→ORC 动因 + 本地模式调优（Q37-Q40）
5. **ADS 指标**：GMV/复购率/RFM 口径，复购率 SQL 一句话，RFM 8 类分法（Q41-Q45）
6. **调度**：DAG 拓扑 + 幂等三层 + 失败重试告警 + 真实修复案例（Q46-Q50）
7. **看板**：Superset 打开 mall_dw，指着 GMV 趋势和 RFM 饼图讲"数仓加工终点是业务可读"（Q51-Q54）
8. **收尾话术**：已知短板（单机伪分布式/告警占位）+ 升级路径（DS 调度/ClickHouse 加速）

---

*文档生成时间：2026-08-21 · 阶段一至九全部交付完毕*
